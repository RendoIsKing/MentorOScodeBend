# ── Backend Environment Variables ──────────────────────────

# Supabase (PostgreSQL) — primary database
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_ANON_KEY=eyJ...
SUPABASE_SERVICE_ROLE_KEY=eyJ...
DATABASE_URL=postgresql://postgres:password@db.your-project.supabase.co:5432/postgres

# MongoDB (legacy — only needed for data migration script)
# DB_URL=mongodb://127.0.0.1:27017/mentoros
# MONGO_URI=mongodb://127.0.0.1:27017/mentoros

# Server port
PORT=3006

# Auth secrets (legacy — Supabase handles auth now)
# SESSION_SECRET=change-me-session-secret
# JWT_SECRET=change-me-jwt-secret

# Frontend origin (CORS + cookie domain)
FRONTEND_ORIGIN=http://localhost:3002

# Cookie config (production)
# SESSION_SAMESITE=none
# SESSION_SECURE=true
# SESSION_COOKIE_DOMAIN=.yourdomain.com

# Sentry DSN for error tracking (optional)
SENTRY_DSN=

# Stripe keys
STRIPE_SECRET_KEY=sk_test_...
STRIPE_PUBLISHABLE_KEY=pk_test_...
STRIPE_WEBHOOK_SECRET=whsec_...
STRIPE_CURRENCY=nok

# OpenAI API key (for AI mentor / RAG / vision)
OPENAI_API_KEY=sk-...

# ElevenLabs (for Coach Majen's cloned voice TTS)
# Model: Eleven v3 (model_id: eleven_v3) — most expressive, supports 70+ languages
# Language: Norwegian Bokmål (nb) — hardcoded in tts.routes.ts
# Setup:
#   1. Create account at https://elevenlabs.io (Starter plan minimum, Creator recommended)
#   2. Clone Majen's voice: Voices > Add Voice > Instant Voice Clone > upload voice samples
#   3. IMPORTANT: Use Norwegian audio clips for best Norwegian output
#   4. Copy API key: Profile > API Keys
#   5. Copy Voice ID: Voices > click your cloned voice > copy Voice ID
ELEVENLABS_API_KEY=
ELEVENLABS_VOICE_ID=

# D-ID (for Majen video avatar with lip-sync)
# 1. Create account at https://www.d-id.com
# 2. Go to Account Settings > API Key > Generate new key
# Avatar images are uploaded via Mentor Settings UI → stored in Supabase Storage
DID_API_KEY=

# Firebase service account JSON (stringified JSON from Firebase console)
# Used for push notifications. Get from Firebase > Project settings > Service accounts > Generate new private key
# Paste the entire JSON as one line:
FIREBASE_SERVICE_ACCOUNT_JSON=

# Upload directory (defaults to ./uploads)
# UPLOAD_ROOT=./uploads

# Google OAuth (for Google login)
GOOGLE_OAUTH_CLIENT_ID=...

# Admin email(s) for auto-promotion (comma-separated)
# ADMIN_EMAILS=admin@example.com

# Platform subscription plan IDs (UUIDs from subscription_plans table)
# PLATFORM_SUBSCRIPTION_PLAN_ID=
# PLATFORM_FREE_SUBSCRIPTION_PLAN_ID=
